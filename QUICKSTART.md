# Быстрый старт

## За 5 минут

### 1. Подготовка

```bash
# Клонируйте репозиторий
cd /Users/mkveisg1/projects/mine/istra-watch

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate

# Установите зависимости
make install
```

### 2. Настройка базы данных

```bash
# Создайте базу данных
createdb istra_watch

# Скопируйте и настройте .env
cp .env.example .env
nano .env
```

Минимальные настройки в `.env`:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
TELEGRAM_ADMIN_IDS=ваш_telegram_id
WEBHOOK_URL=https://ваш-ngrok-url.ngrok.io
MINI_APP_URL=https://ваш-ngrok-url.ngrok.io/miniapp
DB_PASSWORD=ваш_пароль_postgres
YANDEX_MAPS_API_KEY=ваш_ключ_яндекс_карт
```

### 3. Применение миграций

```bash
make migrate
```

### 4. Настройка ngrok (для локальной разработки)

В отдельном терминале:
```bash
ngrok http 8443
```

Скопируйте URL из ngrok и обновите `WEBHOOK_URL` и `MINI_APP_URL` в `.env`

### 5. Запуск

```bash
make run
```

## Первые шаги

### Для администратора

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Создайте пример Excel файла:
   ```bash
   make create-example
   ```
4. Нажмите "📤 Загрузить сотрудников" и отправьте файл
5. Нажмите "📱 Открыть мини-приложение" для просмотра сотрудников

### Для сотрудника

1. Попросите администратора добавить вас через Excel
2. Откройте бота
3. Отправьте `/start`
4. Нажмите "📱 Открыть мини-приложение"
5. Используйте кнопки для отметки прихода/ухода

## Структура проекта

```
istra-watch/
├── bot/                    # Код бота
│   ├── api/               # API для мини-приложения
│   ├── handlers/          # Обработчики команд
│   ├── keyboards/         # Клавиатуры
│   ├── migrations/        # Миграции БД
│   ├── models/            # Модели данных
│   ├── services/          # Бизнес-логика
│   └── utils/             # Утилиты
├── frontend/              # Фронтенд мини-приложения
│   ├── static/
│   │   ├── css/
│   │   └── js/
│   └── index.html
├── scripts/               # Вспомогательные скрипты
└── main.py               # Точка входа
```

## Основные команды

```bash
make install              # Установка зависимостей
make migrate              # Применение миграций
make migrate-rollback     # Откат миграций
make run                  # Запуск бота
make create-example       # Создание примера Excel
```

## API Endpoints

- `POST /api/auth` - Аутентификация пользователя
- `GET /api/employees?date=YYYY-MM-DD` - Список сотрудников за дату
- `GET /api/records/{id}` - Детали записи
- `POST /api/records` - Создание записи

## Полезные ссылки

- Полная документация: [README.md](README.md)
- Развертывание: [DEPLOYMENT.md](DEPLOYMENT.md)
- Тестирование: [TESTING.md](TESTING.md)

## Получение токенов и ключей

### Telegram Bot Token

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

### Ваш Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Нажмите "Start"
3. Скопируйте ваш ID

### Яндекс.Карты API Key

1. Перейдите на [Яндекс.Карты API](https://developer.tech.yandex.ru/)
2. Войдите или зарегистрируйтесь
3. Создайте ключ для Geocoder API

## Troubleshooting

### Бот не отвечает

Проверьте:
- Токен бота правильный
- Webhook установлен (смотрите логи)
- ngrok запущен и URL актуален

### База данных не подключается

Проверьте:
- PostgreSQL запущен: `pg_isready`
- База создана: `psql -l | grep istra_watch`
- Пароль правильный в `.env`

### Мини-приложение не открывается

Проверьте:
- MINI_APP_URL правильный
- URL использует HTTPS
- Файлы в frontend/ существуют

## Поддержка

Если что-то не работает:
1. Проверьте логи
2. Ознакомьтесь с [TESTING.md](TESTING.md)
3. Создайте issue с подробным описанием проблемы

