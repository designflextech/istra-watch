# Руководство по тестированию

## Ручное тестирование

### 1. Тестирование функций администратора

#### Запуск бота

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Убедитесь, что видите кнопки:
   - "📤 Загрузить сотрудников"
   - "📱 Открыть мини-приложение"

#### Загрузка Excel файла

1. Создайте пример файла:
   ```bash
   make create-example
   ```
2. Нажмите "📤 Загрузить сотрудников"
3. Отправьте созданный файл `example_employees.xlsx`
4. Проверьте, что получили сообщение об успешной обработке

#### Просмотр сотрудников через мини-приложение

1. Нажмите "📱 Открыть мини-приложение"
2. Убедитесь, что мини-приложение открылось
3. Проверьте, что отображается список сотрудников
4. Измените дату и убедитесь, что список обновляется
5. Нажмите на сотрудника и проверьте детали записи

### 2. Тестирование функций пользователя

#### Регистрация пользователя

1. Загрузите Excel с вашим Telegram handle
2. Или добавьте пользователя вручную в БД:
   ```bash
   python scripts/init_admin.py <your_telegram_id> "Ваше Имя" "@your_handle"
   ```

#### Открытие бота

1. Откройте бота (с аккаунта, который есть в базе)
2. Отправьте `/start`
3. Убедитесь, что видите кнопку "📱 Открыть мини-приложение"

#### Отметка о приходе

1. Нажмите "📱 Открыть мини-приложение"
2. Нажмите "✅ Отметиться о приходе"
3. Разрешите доступ к геолокации
4. Убедитесь, что местоположение определилось
5. Добавьте комментарий (опционально)
6. Нажмите "Сохранить"
7. Проверьте, что получили сообщение об успехе

#### Отметка об уходе

1. Нажмите "📱 Открыть мини-приложение"
2. Нажмите "🚪 Отметиться об уходе"
3. Разрешите доступ к геолокации
4. Убедитесь, что местоположение определилось
5. Добавьте комментарий (опционально)
6. Нажмите "Сохранить"
7. Проверьте, что получили сообщение об успехе

### 3. Тестирование API

#### Аутентификация

```bash
curl -X POST http://localhost:8443/api/auth \
  -H "Content-Type: application/json" \
  -d '{"telegram_id": 123456789}'
```

Ожидаемый ответ:
```json
{
  "is_admin": true,
  "user": {
    "id": 1,
    "name": "Иван Иванов",
    "telegram_id": 123456789,
    ...
  }
}
```

#### Получение списка сотрудников

```bash
curl http://localhost:8443/api/employees?date=2024-01-01
```

#### Создание записи

```bash
curl -X POST http://localhost:8443/api/records \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 1,
    "type": "arrival",
    "latitude": 55.7558,
    "longitude": 37.6173,
    "comment": "Тестовая запись"
  }'
```

#### Получение деталей записи

```bash
curl http://localhost:8443/api/records/1
```

### 4. Тестирование миграций

#### Применение миграций

```bash
python -m bot.migrations.run migrate
```

Проверьте, что:
- Таблицы созданы в БД
- Индексы созданы
- Нет ошибок

#### Откат миграций

```bash
python -m bot.migrations.run rollback --steps=1
```

Проверьте, что:
- Таблица удалена
- Запись в таблице `migrations` удалена

### 5. Тестирование интеграции с Яндекс.Картами

Проверьте в логах, что при создании записи:
1. Выполняется запрос к Яндекс.Картам
2. Получен корректный адрес
3. Адрес сохранен в базу данных

## Автоматизированное тестирование (TODO)

Для будущего расширения рекомендуется добавить:

### Unit тесты

Создайте `tests/test_models.py`:
```python
import pytest
from bot.models.user import User

def test_user_creation():
    user = User.create(
        name="Test User",
        telegram_handle="@testuser"
    )
    assert user.id is not None
    assert user.name == "Test User"
```

### Интеграционные тесты

Создайте `tests/test_api.py`:
```python
import pytest
from aiohttp.test_utils import AioHTTPTestCase
from main import create_app

class TestAPI(AioHTTPTestCase):
    async def get_application(self):
        return create_app()
    
    async def test_auth(self):
        resp = await self.client.post('/api/auth', json={
            'telegram_id': 123456789
        })
        assert resp.status == 200
```

### Запуск тестов

```bash
pytest tests/
```

## Контрольный список перед релизом

- [ ] Все миграции применяются без ошибок
- [ ] Бот отвечает на команду `/start`
- [ ] Админ может загрузить Excel файл
- [ ] Админ видит список сотрудников в мини-приложении
- [ ] Пользователь может создать отметку о приходе
- [ ] Пользователь может создать отметку об уходе
- [ ] Геолокация определяется корректно
- [ ] Адрес определяется через Яндекс.Карты
- [ ] Webhook работает корректно
- [ ] Мини-приложение открывается
- [ ] Все API endpoints работают
- [ ] Логи пишутся корректно
- [ ] База данных работает стабильно

## Известные ограничения

1. Геолокация работает только при открытии мини-приложения по HTTPS
2. Яндекс.Карты API имеет лимиты на количество запросов
3. Excel файл должен быть в формате .xlsx
4. Даты в админ-панели ограничены 1 месяцем назад

## Reporting Issues

При обнаружении проблемы укажите:
1. Шаги для воспроизведения
2. Ожидаемое поведение
3. Фактическое поведение
4. Логи (если доступны)
5. Версию Python
6. Версию python-telegram-bot

