# Генератор отчетов о дисциплине сотрудников

## Описание

Система генерации PDF отчетов о дисциплине сотрудников включает анализ записей о приходах и уходах сотрудников за указанный период.

## Возможности

- 📊 Сводная статистика по всем сотрудникам
- 👥 Персональная статистика по каждому сотруднику
- 🧭 Аналитика и дисциплина (топ сотрудников, средние значения)
- 📝 Выводы и рекомендации
- 📄 Генерация PDF с поддержкой кириллицы

## Структура отчета

### 1. Заголовок и основная информация
- Период отчета
- Дата формирования
- Рабочее время (09:00 - 18:00)

### 2. Сводные показатели
- Всего сотрудников
- Среднее время прихода/ухода
- Количество рабочих дней в периоде
- Количество опозданий и ранних уходов
- Среднее количество комментариев
- Количество отметок с фото

### 3. Персональная статистика сотрудников
Таблица со следующими колонками:
- Имя сотрудника
- Количество отметок
- Среднее время прихода
- Среднее время ухода
- Количество опозданий (после 09:00)
- Количество ранних уходов (до 18:00)
- Количество фото
- Количество комментариев

### 4. Аналитика и дисциплина
- Топ-3 самых пунктуальных сотрудников
- Топ-3 по опозданиям
- Среднее опоздание в минутах
- Средний ранний уход в минутах
- Самые частые нарушения

### 5. Вывод и рекомендации
- Общая оценка дисциплины
- Рекомендации по улучшению

## Использование

### Через скрипт командной строки

```bash
# Генерация отчета за период
python scripts/generate_report.py 2025-10-01 2025-10-31

# Генерация отчета за текущий месяц (по умолчанию)
python scripts/generate_report.py
```

### Через Python код

```python
from datetime import date
from bot.services.report_generator import generate_discipline_report

# Генерация отчета
date_from = date(2025, 10, 1)
date_to = date(2025, 10, 31)

pdf_buffer = generate_discipline_report(date_from, date_to)

# Сохранение в файл
with open('report.pdf', 'wb') as f:
    f.write(pdf_buffer.getvalue())
```

### Через API endpoint

**Endpoint:** `GET /api/reports/discipline`

**Параметры запроса:**
- `date_from` (обязательный) - дата начала периода в формате YYYY-MM-DD
- `date_to` (обязательный) - дата окончания периода в формате YYYY-MM-DD

**Требования:**
- Требуется аутентификация через Telegram WebApp
- Доступ только для администраторов

**Пример запроса:**

```bash
curl -X GET "http://localhost:8080/api/reports/discipline?date_from=2025-10-01&date_to=2025-10-31" \
  -H "Authorization: tma <init_data>"
```

**Ответ:**
- При успехе: PDF файл с отчетом
- При ошибке: JSON с описанием ошибки

**Возможные ошибки:**
- `401` - Неавторизованный запрос
- `403` - Доступ запрещен (не администратор)
- `400` - Неверные параметры запроса
- `500` - Внутренняя ошибка сервера

## Примеры использования в frontend

```javascript
// JavaScript пример
async function downloadReport(dateFrom, dateTo) {
    const initData = window.Telegram.WebApp.initData;
    
    const url = `/api/reports/discipline?date_from=${dateFrom}&date_to=${dateTo}`;
    
    const response = await fetch(url, {
        headers: {
            'Authorization': `tma ${initData}`
        }
    });
    
    if (response.ok) {
        const blob = await response.blob();
        const downloadUrl = window.URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = downloadUrl;
        a.download = `report_${dateFrom}_${dateTo}.pdf`;
        document.body.appendChild(a);
        a.click();
        a.remove();
    } else {
        const error = await response.json();
        console.error('Ошибка:', error);
    }
}

// Использование
downloadReport('2025-10-01', '2025-10-31');
```

## Технические детали

### Зависимости
- `reportlab` - генерация PDF
- `psycopg2-binary` - работа с PostgreSQL
- `python-dotenv` - работа с переменными окружения
- Шрифты DejaVu Sans (для поддержки кириллицы)

### Установка шрифтов

Для корректного отображения кириллицы в PDF необходимо установить шрифты DejaVu:

```bash
# Скачивание и установка шрифтов в проект
mkdir -p fonts
cd fonts
curl -L -O https://github.com/dejavu-fonts/dejavu-fonts/releases/download/version_2_37/dejavu-fonts-ttf-2.37.tar.bz2
tar -xjf dejavu-fonts-ttf-2.37.tar.bz2
mv dejavu-fonts-ttf-2.37/ttf/*.ttf .
rm -rf dejavu-fonts-ttf-2.37 dejavu-fonts-ttf-2.37.tar.bz2
cd ..
```

**Примечание:** Директория `fonts/` добавлена в `.gitignore` и не коммитится в репозиторий. При развертывании на сервере необходимо выполнить установку шрифтов.

### Конфигурация
Рабочее время задается в классе `DisciplineReportGenerator`:
- `WORK_START = time(9, 0)` - начало рабочего дня
- `WORK_END = time(18, 0)` - окончание рабочего дня

### Расчет статистики

**Опоздание** - приход после 09:00  
**Ранний уход** - уход до 18:00  
**Рабочие дни** - понедельник-пятница в указанном периоде

### Особенности
- Исключаются администраторы из отчета
- Учитываются только первые записи прихода и ухода за день
- PDF генерируется с поддержкой кириллицы

## Разработка

### Структура кода

```
bot/services/
├── report_generator.py    # Главный класс генератора
└── __init__.py           # Экспорты

scripts/
└── generate_report.py    # CLI скрипт
```

### Класс DisciplineReportGenerator

Основные методы:
- `__init__(date_from, date_to)` - инициализация
- `generate_pdf(output_path=None)` - генерация PDF
- `_get_employees_data()` - получение данных сотрудников
- `_calculate_employee_stats(employee)` - расчет статистики сотрудника
- `_calculate_summary_stats(employees_stats)` - расчет сводной статистики

### Добавление новых разделов

Чтобы добавить новый раздел в отчет:

1. Добавьте метод расчета данных:
```python
def _calculate_new_section_data(self, employees_stats):
    # Ваша логика
    return data
```

2. Добавьте секцию в метод `generate_pdf()`:
```python
# Новый раздел
story.append(Paragraph("Новый раздел", heading_style))
# Добавьте таблицы, графики и т.д.
story.append(Spacer(1, 0.7*cm))
```

## FAQ

**Q: Как изменить рабочее время?**  
A: Измените константы `WORK_START` и `WORK_END` в классе `DisciplineReportGenerator`.

**Q: Как добавить выходные дни?**  
A: Модифицируйте метод `_get_work_days_count()` для исключения праздников.

**Q: Почему эмодзи не отображаются в PDF?**  
A: Стандартные шрифты PDF не поддерживают эмодзи. Можно использовать специализированные шрифты с поддержкой Unicode эмодзи.

**Q: Как настроить оценку дисциплины?**  
A: Измените логику в секции "Вывод" метода `generate_pdf()`, где определяется `discipline_level`.

## Лицензия

Часть проекта istra-watch.

